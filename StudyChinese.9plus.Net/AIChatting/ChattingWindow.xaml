<Window x:Class="StudyChinese._9plus.Net.AIChatting.ChattingWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Управление викторинами" Height="528" Width="824"
        Background="#FFF5F5"
        WindowStyle="None"
        AllowsTransparency="True"
        ResizeMode="NoResize"
        WindowState="Maximized"
        KeyDown="SendButton_KeyDown">

    <Window.Resources>
        <Style x:Key="BackButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Width" Value="50"/>
            <Setter Property="Height" Value="50"/>
            <Setter Property="FontSize" Value="24"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Foreground" Value="#333333"/>
            <Setter Property="HorizontalAlignment" Value="Left"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="25">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E0E0E0"/>
                    <Setter Property="Foreground" Value="#666666"/>
                </Trigger>
            </Style.Triggers>
        </Style>
        <Style x:Key="BorderButton" TargetType="Button">
            <Setter Property="Background" Value="Transparent"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <!-- Border, на который действует Trigger -->
                            <Border x:Name="border"
                            Background="{TemplateBinding Background}"
                            CornerRadius="40"
                            BorderThickness="1"
                            BorderBrush="Blue"/>

                            <ContentPresenter HorizontalAlignment="Center"
                                      VerticalAlignment="Center" />
                        </Grid>

                        <ControlTemplate.Triggers>

                            <!-- Наведение -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border"
                                Property="Background"
                                Value="#FFDEEFFF"/>
                            </Trigger>

                            <!-- Нажатие -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border"
                                Property="Background"
                                Value="#FFBAD7FF"/>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <!-- Цвета -->
        <SolidColorBrush x:Key="PrimaryColor" Color="#D32F2F"/>
        <SolidColorBrush x:Key="PrimaryLight" Color="#FF6659"/>
        <SolidColorBrush x:Key="PrimaryDark" Color="#9A0007"/>
        <SolidColorBrush x:Key="BorderGray" Color="#CCCCCC"/>

        <!-- Тень -->
        <DropShadowEffect x:Key="CardShadow" 
                          Color="#000" 
                          Direction="315"
                          ShadowDepth="2" 
                          Opacity="0.15"
                          BlurRadius="12"/>

        <!-- Основной стиль ToggleButton -->
        <Style x:Key="HSKToggleButton" TargetType="ToggleButton">
            <Setter Property="Background" Value="White"/>
            <Setter Property="Foreground" Value="#333"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="16"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="BorderBrush" Value="{StaticResource BorderGray}"/>
            <Setter Property="Padding" Value="10 6"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="HorizontalContentAlignment" Value="Center"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>

            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ToggleButton">
                        <Border x:Name="border"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>

                        <ControlTemplate.Triggers>

                            <!-- Наведение -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryLight}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
                            </Trigger>

                            <!-- Выбрана -->
                            <Trigger Property="IsChecked" Value="True">
                                <Setter TargetName="border" Property="Background" Value="{StaticResource PrimaryDark}"/>
                                <Setter Property="Foreground" Value="White"/>
                                <Setter TargetName="border" Property="BorderBrush" Value="{StaticResource PrimaryDark}"/>
                                <Setter TargetName="border" Property="Effect">
                                    <Setter.Value>
                                        <DropShadowEffect BlurRadius="10" ShadowDepth="2" Opacity="0.3"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                            <!-- Нажатие -->
                            <Trigger Property="IsPressed" Value="True">
                                <Setter TargetName="border" Property="RenderTransform">
                                    <Setter.Value>
                                        <ScaleTransform ScaleX="0.96" ScaleY="0.96"/>
                                    </Setter.Value>
                                </Setter>
                            </Trigger>

                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <!-- Эксклюзивная группа (только одна выбрана) -->
        <Style x:Key="ExclusiveToggleGroup" TargetType="ToggleButton" BasedOn="{StaticResource HSKToggleButton}">
            <EventSetter Event="Checked" Handler="ToggleButton_Checked"/>
        </Style>

    </Window.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="95"/>
            <RowDefinition/>
        </Grid.RowDefinitions>

        <!-- Верхняя панель -->
        <Border Grid.Row="0" Background="{StaticResource PrimaryColor}">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="255"/>
                    <ColumnDefinition/>
                    <ColumnDefinition Width="250"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0" Text="HSK汉语学习助手" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="30"/>
                <TextBlock Grid.Column="0" Text="|" HorizontalAlignment="Right" VerticalAlignment="Center" Foreground="White" FontSize="35" Margin="0 0 0 11"/>
                <TextBlock Grid.Column="1" Text="AI对话式中文学习平台" HorizontalAlignment="Center" VerticalAlignment="Center" Foreground="White" FontSize="30"/>
                <Button Grid.Column="2" x:Name="BackButton" Style="{StaticResource BackButton}" Content="←" HorizontalAlignment="Right" Margin="0 0 10 0" Click="BackButton_Click"/>
            </Grid>
        </Border>
        <!-- Основная сетка -->
        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="250"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>
            <!-- Левое меню -->
            <Border BorderThickness="1" BorderBrush="Gray" Padding="20">
                <Grid>
                    <Grid.RowDefinitions>
                        <RowDefinition/>
                        <RowDefinition Height="30"/>
                        <RowDefinition Height="190"/>
                    </Grid.RowDefinitions>

                    <Grid Grid.Column="0" Grid.Row="0">
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <Grid.RowDefinitions>
                            <RowDefinition Height="95"/>
                            <RowDefinition Height="95"/>
                            <RowDefinition Height="95"/>
                        </Grid.RowDefinitions>

                        <ToggleButton Grid.Column="0" Margin="10" Grid.Row="0" Style="{StaticResource ExclusiveToggleGroup}" Content="HSK1"/>
                        <ToggleButton Grid.Column="1" Margin="10" Grid.Row="0" Style="{StaticResource ExclusiveToggleGroup}" Content="HSK2"/>
                        <ToggleButton Grid.Column="0" Margin="10" Grid.Row="1" Style="{StaticResource ExclusiveToggleGroup}" Content="HSK3"/>
                        <ToggleButton Grid.Column="1" Margin="10" Grid.Row="1" Style="{StaticResource ExclusiveToggleGroup}" Content="HSK4"/>
                        <ToggleButton Grid.Column="0" Margin="10" Grid.Row="2" Style="{StaticResource ExclusiveToggleGroup}" Content="HSK5"/>
                        <ToggleButton Grid.Column="1" Margin="10" Grid.Row="2" Style="{StaticResource ExclusiveToggleGroup}" Content="HSK6"/>

                    </Grid>
                    <TextBlock HorizontalAlignment="Center" VerticalAlignment="Center" FontSize="15" Text="Please choose chat mode" Grid.Row="1"/>
                    <Grid Grid.Row="2">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="95"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition/>
                            <ColumnDefinition/>
                        </Grid.ColumnDefinitions>

                        <ToggleButton Grid.Column="0" Margin="10" Grid.Row="0" Style="{StaticResource ExclusiveToggleGroup}" Content="Teacher"/>
                        <ToggleButton Grid.Column="1" Margin="10" Grid.Row="0" Style="{StaticResource ExclusiveToggleGroup}" Content="Friend"/>

                    </Grid>
                </Grid>
            </Border>

            <!-- Правая часть -->
            <Grid Grid.Column="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="381*"/>
                    <ColumnDefinition Width="193*"/>
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition/>
                    <RowDefinition Height="85"/>
                </Grid.RowDefinitions>
                <ScrollViewer VerticalScrollBarVisibility="Auto" 
                 HorizontalScrollBarVisibility="Disabled" Grid.ColumnSpan="2">
                    <StackPanel x:Name="ChatWindow" Grid.Row="0" >
                    </StackPanel>
                </ScrollViewer>
                <Border Grid.Row="1" BorderBrush="Gray" BorderThickness="1" Grid.ColumnSpan="2">
                    <Border BorderBrush="Black" BorderThickness="1" CornerRadius="30" Margin="12">
                        <Grid>
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition/>
                                <ColumnDefinition Width="65"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="1" BorderBrush="Blue" BorderThickness="1" Margin="5" CornerRadius="40">

                                <Button Content="отправить" FontSize="9"
                                    Style="{StaticResource BorderButton}"
                                    HorizontalAlignment="Stretch"
                                    VerticalAlignment="Stretch" Click="UserMessageSend_Click"
                                    />
                            </Border>
                            <TextBox x:Name="userMessageField" Grid.Column="0"
                                    Text="Write, smth"
                                    VerticalAlignment="Center"
                                    Margin="13 0 10 0"
                                    Background="#FFF5F5"
                                    BorderThickness="0"
                                     FontSize="36"/>
                        </Grid>
                    </Border>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
